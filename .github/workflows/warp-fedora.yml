name: Warp Fedora

on:
  push:
    paths:
      - "packages/warp/**"
      - ".github/workflows/warp-fedora.yml"
      - "README.md"
      - "README.ru.md"
  pull_request:
    paths:
      - "packages/warp/**"
      - ".github/workflows/warp-fedora.yml"
      - "README.md"
      - "README.ru.md"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        run: docker build -f packages/warp/docker/Dockerfile.fedora -t warp:fedora packages/warp

      - name: Smoke test version
        run: docker run --rm warp:fedora --version

      - name: Smoke test help
        run: docker run --rm warp:fedora --help >/dev/null
